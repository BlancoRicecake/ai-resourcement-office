# UI·UX 자문가는 필수 API 키가 없다.
# 화면 리뷰·기획 자문 판단은 이 번들을 구동하는 호스트 코딩 에이전트/LLM이 맡는다.
# URL 수집·스크린샷 해석도 호스트 런타임의 능력을 그대로 쓴다 — 별도 키가 필요 없다.
#
# 아래는 전부 선택 사항이다. 없어도 자문가는 붙여넣은 화면 묘사·HTML·기획안만으로 작동한다.

# 선택: 사용하는 모델 제공자 키 (플랫폼에 따라 불필요할 수 있음)
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
