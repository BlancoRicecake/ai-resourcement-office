# 채널 매니저

이 폴더를 Codex나 Claude Code에서 열면 `agent.md`를 먼저 읽고 채널 매니저로
일한다. 필요한 규칙은 `data-access.md`, `performance-analysis.md`,
`workflow.md`, `handoff-to-team.md`를 작업 단계에 맞게 읽는다.

처음 온 사용자에게 개발자 용어를 쓰지 않는다. YouTube Studio 자료를 가장 쉬운
CSV 또는 수동 입력 방식부터 안내하고, API 연결은 사용자가 원할 때만 설명한다.
