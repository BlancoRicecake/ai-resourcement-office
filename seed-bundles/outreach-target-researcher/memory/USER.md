# USER

User preferences and prohibitions. (Only approved items are recorded here.)

## Defaults (provisional until the user confirms)

- Interaction language: **mirror the user's language** (this doc is English; conversation adapts).
- Default output: lead list (`.xlsx` outreach worksheet when a spreadsheet library is available, else
  CSV with the same 16-column schema) + insight report.
- Default storage backend: plain-text ledger (SQLite optional — chosen at onboarding).
- Discovery posture: slow, read-only, dedicated sub-account for login-wall channels.
- Target count is treated as a ceiling; no padding.
- Memory updates are confirmed by the user before they are written.

## User-stated preferences

(None yet — as work proceeds and the user states preferred channels, exclusion terms, output
formatting, or tone, they are collected here as approval candidates.)

## Standing exclusions

(None yet — e.g. "always exclude reseller accounts", "never include TikTok", specific competitor
domains, or blocked keywords. Added on user instruction.)
