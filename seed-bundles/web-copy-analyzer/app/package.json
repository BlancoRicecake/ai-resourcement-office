{
  "name": "@ai-factory/web-copy-analyzer",
  "version": "0.1.0",
  "description": "Web Copy Analyzer — conversion-copy consultant. Isomorphic core (parse/score/prep/compare) + MCP server + plain CLI, bundled as a single-file, install-free download (see ../worker, ../plugin).",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=18.17.0"
  },
  "bin": {
    "web-copy-analyzer-mcp": "./dist/mcp.js",
    "web-copy-analyzer-cli": "./dist/cli.js"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "npm run typecheck && node scripts/build.mjs && npm run gen:agent",
    "gen:agent": "node scripts/gen-agent.mjs",
    "test": "node --import tsx --test core/__tests__/*.test.ts mcp/__tests__/*.test.ts",
    "start:mcp": "node dist/mcp.js",
    "start:cli": "node dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.28.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0"
  }
}
