# Skill: [작업 이름]

[워커 이름]이 [핵심 작업]을 수행할 때 따르는 절차.

## 절차

1. 입력을 확인한다. 필수 값이 없으면 사용자에게 묻는다.
2. 입력에 개인정보/기밀이 보이면 실행을 멈추고 사용자에게 정리를 요청한다.
3. 미니 SaaS CLI로 작업을 실행한다:

   ```bash
   python app/app.py --cli <입력 파일> -o <출력 파일>
   ```

4. 생성된 결과를 읽고 다음을 점검한다:
   - [도메인별 검증 항목 1]
   - [도메인별 검증 항목 2]
5. 점검 결과와 함께 결과물을 사용자에게 전달한다.

## 주의

- API 키가 없으면 자동으로 모의(mock) 모드로 실행된다. 결과 전달 시 모드를 명시한다.
- [도메인별 주의사항]
