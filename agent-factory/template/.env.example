# 이 결정론 도구는 기본적으로 API 키가 필요 없습니다(판단은 호스트
# 코딩 에이전트/LLM이 담당). 이 번들이 실제로 읽는 env 변수만 아래에
# 남기고, 없으면 이 파일을 비워두는 대신 "키 불필요"를 README/bundle.json
# requiredKeys에 명시하세요.

# 예시(필요한 경우에만 유지):
# OPENAI_API_KEY=
